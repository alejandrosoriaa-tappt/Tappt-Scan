# Handoff — 2026-08-31

_Para quien retome, en esta o en otra sesión._

Estado del repo al cierre: **todo fusionado a `main`**. No quedan ramas con
trabajo pendiente de integrar.

## Qué se hizo en estas sesiones (26 al 31 de agosto)

### Código

| Commit | Qué resuelve |
|---|---|
| `957d060` | El botón **"Es otra cosa"** de WhatsApp respondía el saludo genérico en vez de reclasificar. Ahora guarda estado pendiente (Map, ventana de 30 min), reclasifica con la pista del usuario y **mueve** el archivo en Drive sin re-subirlo. |
| `6de8577` | Sección **`08 · Educación`** + **nivel de persona** en la ruta. `08 · Educación / Patricio Soria / Colegiaturas / Colegio / 2026`. Nivel dinámico: ningún nombre está escrito en el código. |

Archivos nuevos que conviene conocer: `services/vision.js →
reclasificarConPista()`, `services/drive.js → moverArchivo()`,
`services/taxonomia.js → usaPersona()`.

### Investigación de mercado

Está toda en `CLAUDE.md`, sección "Sesiones 2026-08-26 al 31". Lo esencial:

- **México es el 59% de las descargas de Scan Hero** (Appfigures, jul 2026).
  Una estimación previa usaba el 2.1% que México pesa en el mercado global
  de apps — estaba mal por ~28x para esta categoría.
- **Categoría de escaneo en México ≈ $8–15M USD/año.** El 5% son
  $400–750K USD (~$7.4–13.9M MXN).
- **Problema de precio**: capa gratis 18x más restrictiva que la del
  competidor (5/mes vs 3/día) y plan anual a una quinta parte ($299 vs
  ~$1,399 MXN).

## ⚠️ Pendiente operativo que bloquea producción

```sql
alter table scan_documents add column if not exists persona text;
```

**Sin esto la reclasificación con persona falla en producción.** Está al
final de `scan_schema.sql`. Es lo primero que hay que correr.

## Frentes abiertos, por orden

### Técnicos

1. **Regresión de nitidez de cámara** — sigue abierta. Ver en `CLAUDE.md` la
   sección "🔴 ABIERTO (2026-08-20)". La hipótesis del zoom (0.5x → 0.6x) no
   se confirmó; el sospechoso fuerte es la **selección de cámara ultra
   angular por label** en `CamaraDoc.web.js`. Otra sesión pusheó
   `18aaa71 fix(camara): A/B primary vs ultra-wide con gate de resolución`
   — **revisar qué hizo antes de duplicar trabajo.**
   Falta guardar el diagnóstico de CÁMARA en el fixture (hoy solo guarda el
   del detector), que es el dato que permitiría confirmarlo.
2. **Banco de fixtures: 16/20.** Faltan #7 (documento cortado por el borde
   del cuadro) y #10 (dos hojas de verdad encimadas). Ver
   `scanner/fixtures/fotos/README.md`.
3. **Caso abierto del compuesto**: cinco superficies distintas ya muestran
   el mismo patrón (DocQuad acierta, OpenCV falla, el desacuerdo degrada a
   parcial). Hay evidencia acumulada para revisar la regla, pero **no se
   toca sin medir contra el banco completo**.

### Producto y negocio

4. **Subir la capa gratis** a ~1 escaneo diario. El cambio de mayor impacto
   y el más barato: hoy el usuario topa el límite antes de llegar al momento
   en que se enamora (ver su archivo aparecer solo en Drive).
5. **Diseñar el paquete Agenda + Scan.** La idea: al usuario de TapptScan se
   le regala Tappt Agenda — agenda, calendario, Drive y scanner en el mismo
   WhatsApp. Es lo único que justifica subir a $79–89 MXN/semana.
   ⚠️ Es decisión **comercial**, no fusión de repos: la regla permanente de
   verticales sigue en pie. Falta definir cómo se vincula una cuenta entre
   `tappt-backend` y `tappt-scan`.
6. **Producir la Ola 1 de fichas de App Store** (3 juegos de capturas):
   Facturas y CFDI, Trámites, Colegiaturas. Plan completo con copy,
   keywords y capturas:
   https://claude.ai/code/artifact/66b7bbf7-7e6b-426e-a039-0b5eb90d7411
7. **Buscar el D-U-N-S de Tappt** en `developer.apple.com/enroll/duns-lookup/`
   — 2 minutos, y si ya existe ahorra hasta 30 días hábiles de trámite.
8. **Evaluar Appfigures o Sensor Tower** con desglose por país. Es lo único
   que convertiría la estimación de mercado en dato duro.

## Notas de método

- Hubo **más de una sesión de Claude trabajando en paralelo** durante estos
  días. Antes de empezar: `git fetch` y revisar qué llegó. Fusionar con
  merge, nunca rebase ni force-push sobre trabajo ajeno.
- Varias conclusiones de estas sesiones **se corrigieron a sí mismas con
  datos** (el 2.1% → 59%, el orden del nivel de persona, la estimación de
  descargas necesarias). Cuando un número venga de una analogía y no de una
  fuente, marcarlo como supuesto.
- La disciplina del banco de fixtures sigue vigente: **ningún cambio al
  detector se justifica con "se ve bien"**, se justifica con IoU contra
  ground truth.
